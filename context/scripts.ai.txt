# AI context — mac-media-scripts
Репозиторий: /Users/lis/DEV/scripts → https://github.com/LisKorzun/mac-media-scripts
Читай этот файл перед правками. Для людей: [[README.md]]. ТЗ: [[specs/to-mp4.txt]], [[specs/cut-mp4.txt]].

## Зачем
Bash-скрипты для медиа на macOS. Сюда только инструменты; готовые файлы — в библиотеки владельца.

Приоритет: **видео фильмов** (контейнер + дорожки, звук внутри файла). Чистый аудио и картинки — позже; не путать sound-in-video с отдельным mp3.

## Раскладка
| Путь          | Зачем                                              |
| ------------- | -------------------------------------------------- |
| `bin/`        | запускаемые скрипты (kebab-case: `to-mp4`, …)      |
| `lib/`        | общие куски shell, если появятся                   |
| `inprogress/` | временные медиа; после работы очищать              |
| `specs/`      | ТЗ: `specs/<script-name>.txt`                      |
| `context/`    | `scripts.ai.txt`                                   |
| корень        | `README.md`, `.gitignore`                          |

Флоу: скрипты в `bin/` (не копировать в репо медиа). Видео — где угодно; часто `inprogress/`.
Запуск: PATH на `~/DEV/scripts/bin` в `~/.zshrc` → `cd` в папку с видео → `to-mp4` / `cut-mp4` (см. [[README.md]]).
Скрипт смотрит только **текущую папку Терминала**; выход рядом с исходником → готовое в библиотеку → `inprogress/` очистить при необходимости.
`to-mp4` — конвертация (mkv/… → mp4). `cut-mp4` — вырезка из **уже готового** mp4 (stream copy).
Без PATH: относительный путь к `bin/to-mp4` или копия скрипта в рабочую папку (удалить после).
Сессии-подпапки в `inprogress/` — позже, если понадобится.

## Аудитория
Владелец + ассистенты (Cursor / другие ИИ).

## Устройства
| Устройство        | ОС / роль                             |
| ----------------- | ------------------------------------- |
| iMac              | macOS 26.6 (arm64) — основная станция |
| MacBook Air       | macOS 26.6 (arm64) — портативная      |
| iPhone 14 Pro Max | iOS                                   |
| Apple Watch       | watchOS                               |

Скрипты: **macOS (zsh/bash, Darwin)**. iPhone/Watch — не цель bash.

## Стек (основные)
| Инструмент | Статус (2026-08-21) | Зачем                    |
| ---------- | ------------------- | ------------------------ |
| Homebrew   | есть `/opt/homebrew`| пакеты                   |
| ffmpeg     | есть 9.0.1          | перекод / контейнеры     |
| ffprobe    | есть (с ffmpeg)     | дорожки / кодеки для UI  |
| ffplay     | есть (с ffmpeg)     | быстрый просмотр         |

`to-mp4` по ТЗ: **libx264 + CRF**, не VideoToolbox.

## План
1. ~~Реализовать `bin/to-mp4` по [[specs/to-mp4.txt]].~~ есть `bin/to-mp4`
2. Запуск через PATH на `bin/` — описано в [[README.md]]; настройка `~/.zshrc` у владельца
3. ~~`bin/cut-mp4` — вырезка из готового mp4 (copy).~~ есть [[specs/cut-mp4.txt]]
4. Позже по запросу: вырезка из оригинала с encode / from–to в to-mp4

Не начинать скрипт без явного «пиши» / рабочей сессии; правки ТЗ — в `specs/`.

## Как писать код
- Bash (или zsh-совместимый), имена в `bin/`: **kebab-case**.
- Минимализм: только запрошенное; без лишних проверок и комментариев, если не просили.
- Утилиты под macOS; не ломать оба Mac без явного согласия.
- Медиа не складировать в репо как библиотеку (`.gitignore` уже режет типичные контейнеры).
- Общее — в `lib/`, когда реально понадобится дублирование.

## Документация
Короткие заметки; ссылки внутри текста (`[[имя]]`), без блоков «похожие ссылки».
